{
  "id": "dialog",
  "name": "TyDialog",
  "category": "unknown",
  "description": "",
  "props": {
    "title": {
      "type": "string",
      "default": "提示",
      "required": false,
      "description": "对话框标题"
    },
    "width": {
      "type": "string",
      "default": "30%",
      "required": false,
      "description": "对话框宽度"
    },
    "top": {
      "type": "string",
      "default": "15vh",
      "required": false,
      "description": "对话框距顶部距离"
    },
    "info": {
      "type": "string",
      "default": null,
      "required": false,
      "description": "提示信息"
    },
    "isUnderLine": {
      "type": "boolean",
      "default": true,
      "required": false,
      "description": "是否显示下划线"
    },
    "isTeleport": {
      "type": "boolean",
      "default": true,
      "required": false,
      "description": "是否使用 teleport 传送"
    },
    "draggable": {
      "type": "boolean",
      "default": true,
      "required": false,
      "description": "是否可拖拽"
    },
    "mask": {
      "type": "boolean",
      "default": true,
      "required": false,
      "description": "是否显示遮罩层"
    }
  },
  "emits": {
    "modelValue": {
      "params": [
        "value: boolean"
      ],
      "description": ""
    }
  },
  "slots": [
    "title",
    "default",
    "footer"
  ],
  "examples": [],
  "capabilities": [],
  "useCases": []
}